services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
      NEXT_PUBLIC_API_URL: http://localhost:5001
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
