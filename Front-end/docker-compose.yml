version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    command: sh -c "npm run dev"

volumes:
  node_modules:
